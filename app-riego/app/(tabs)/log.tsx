import { View, Text } from 'react-native';

export default function LogScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Activity Log — coming in PR3</Text>
    </View>
  );
}
