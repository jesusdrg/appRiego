import { View, Text } from 'react-native';

export default function SchedulesScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Schedules — coming in PR2</Text>
    </View>
  );
}
